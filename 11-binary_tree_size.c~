#include "binary_trees.h"
#include <stdlib.h>

size_t binary_tree_size(const binary_tree_t *tree)
{
	size_t ln, rn;

	if (tree == NULL)
		return (0);
	ln = binary_tree_size(tree->left);
	rn = binary_tree_size(free->right);
	return (ln + rn +1);
}
